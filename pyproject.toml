[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wt4"
version = "0.1.0"
description = "可审计的 MT5 策略验收底座"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
